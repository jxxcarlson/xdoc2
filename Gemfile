source 'https://rubygems.org'

ruby '2.2.3'

gem 'bundler'
gem 'rake'
gem 'hanami',       '0.7.3'
gem 'hanami-model', '~> 0.5'

gem 'pg'

gem 'bcrypt'
gem 'jwt'
gem 'aws-sdk'
gem 'rack-cors', :require => 'rack/cors'

gem 'asciidoctor-latex'
gem 'thread_safe'

gem 'puma'

group :test do
  gem 'minitest'
  gem 'capybara'
end

group :production do
  # gem 'puma'
end
